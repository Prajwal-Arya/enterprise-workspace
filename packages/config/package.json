{
  "name": "@enterprise/config",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "echo build config",
    "lint": "echo lint config",
    "test": "echo test config"
  }
}
